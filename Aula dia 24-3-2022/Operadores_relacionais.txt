//operadores lógicos 

> verdadeiro se o operando à esquerda é maior    A > B
< verdadeiro se o operando à esquerda é menor    A < B
>= Verdadeiro se o operando à esquerda é maior ou igual A >= B
<= Verdadeiro se o operando à esquerda é menor ou iual A <= B
== verdadeiro se os operandos são iguais A==B
!= Verdadeiro se os dois operandos são diferentes A!=B

OU lógico -> ||     A||B
E lógico -> &&      A&&B
NEGAÇÃO -> !        A!B



If statments:

if (expressão booleana)
{
    <Instrução>
}
else
{
    <Instrução>
}

if (numero % 2 == 0){
 printf("numero é par.\n");
}
else{
    printf("Numero é impar.\n");
}